server:
  port: 8081

spring:
  application:
    name: "auth-service" # Changed to lowercase to prevent Eureka/Gateway routing issues

  config:
    import: "optional:configserver:http://localhost:9093/"
